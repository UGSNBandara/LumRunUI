# Copy to .env.local and fill in your MongoDB connection string
MONGO_URL=
DB_NAME=game
COLL_NAME=users

# When deploying on Vercel, set these as Project Environment Variables.
# Optionally set NEXT_PUBLIC_BASE_URL for server-side fetch to absolute URL in serverless contexts.
# NEXT_PUBLIC_BASE_URL=
